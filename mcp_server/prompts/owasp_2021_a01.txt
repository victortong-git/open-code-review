Examine the following code specifically for OWASP 2021 A01: Broken Access Control vulnerabilities:

Focus ONLY on identifying Broken Access Control issues, which include:
1. Violation of principle of least privilege or deny by default
2. Bypassing access control checks by modifying the URL, internal application state, or the HTML page
3. Permitting viewing or editing someone else's account, by providing its unique identifier
4. Accessing API with missing access controls for POST, PUT and DELETE
5. Elevation of privilege - acting as a user without being logged in, or acting as an admin when logged in as a user
6. Metadata manipulation, such as replaying or tampering with JWT, cookies, or hidden fields
7. CORS misconfiguration allowing unauthorized API access
8. Force browsing to authenticated or privileged pages
9. Insecure direct object references exposing implementation objects or sensitive data
10. Missing or improper access control on APIs or endpoints

Type Options specific to A01 issues:
- Authorization Issue
- Authentication Issue
- Insecure Direct Object References
- Missing Access Control
- Privilege Escalation
- CORS Misconfiguration
- JWT/Token Manipulation
- Force Browsing Vulnerability
- Other Access Control Issue

Return all your findings as a JSON object array with these fields:
[
{
    "file_id": (string) the ID of the file being reviewed,
    "type": (string) the type of issue found specifically related to this finding. Use the type options. E.g. xxxxxx,
    "line_number": (integer) the line number where the issue was found,
    "issues_found": (boolean) true if A01 issues were found, false if no A01 issues detected,
    "description": (string) describe the issue detail, 
    "severity": (string) the severity of the issues found (e.g., "critical", "high", "medium", "low"),
    "severity_reason": (string) a brief explanation of the severity level specifically for access control context,
    "recommendation": (array) list of suggestions to specifically address the access control vulnerabilities with sample code examples,
    "code_content": (string) extract part of related code from original code content and add comment to highlight the access control issue
    "status": "new", This value should be "new"
}
]

Requirement for description, it should be details and easy to understand. And it should label in newline to say "This issue is based on OWASP 2021 Top 10 Axx xxxxx to review".

Please add comment with "<--- issue" this highlight the code line having issue in code_content

This is an automated code review submission focused exclusively on OWASP A01: Broken Access Control.
Please return the result in JSON format object only with no other text.
